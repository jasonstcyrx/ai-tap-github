
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Breaking Changes for GitHub REST API",
  "description": "Information about breaking changes introduced in each version of the GitHub REST API",
  "type": "object",
  "properties": {
    "apiVersion": {
      "type": "string",
      "description": "The version of the GitHub REST API",
      "example": "2022-11-28"
    },
    "breakingChanges": {
      "type": "array",
      "description": "List of breaking changes introduced in the specified API version",
      "items": {
        "type": "object",
        "properties": {
          "change": {
            "type": "string",
            "description": "Description of the breaking change"
          }
        },
        "required": [
          "change"
        ]
      }
    }
  },
  "required": [
    "apiVersion",
    "breakingChanges"
  ]
}
